- name: will wait till reachable
  hosts: master
  gather_facts: no
  tasks:
    - name: Wait for system to become reachable
      wait_for_connection:

    - name: Gather facts for the first time
      setup:
- hosts: master
  become: true
  tasks:
    - name: Wait until apt/dpkg lock is free
      shell: |
        fuser /var/lib/dpkg/lock >/dev/null 2>&1 && exit 1
        fuser /var/lib/apt/lists/lock >/dev/null 2>&1 && exit 1
        fuser /var/cache/apt/archives/lock >/dev/null 2>&1 && exit 1
        exit 0
      register: apt_lock_check
      retries: 60
      delay: 10
      until: apt_lock_check.rc == 0

    - name: Update system
      apt:
        update_cache: yes
        upgrade: dist
      retries: 10
      delay: 10

    - name: Disable swap
      command: swapoff -a

    - name: Remove swap entry from fstab
      replace:
        path: /etc/fstab
        regexp: '(\s+swap\s+)'
        replace: '# \1'

    - name: Load required kernel modules
      copy:
        dest: /etc/modules-load.d/k8s.conf
        content: |
          overlay
          br_netfilter

    - name: Configure sysctl
      copy:
        dest: /etc/sysctl.d/k8s.conf
        content: |
          net.bridge.bridge-nf-call-iptables  = 1
          net.bridge.bridge-nf-call-ip6tables = 1
          net.ipv4.ip_forward                 = 1
    - name: Apply sysctl
      command: sysctl --system

    - name: Install containerd
      apt:
        name: containerd
        state: present

    - name: Configure containerd
      shell: |
        mkdir -p /etc/containerd
        containerd config default > /etc/containerd/config.toml
        sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml
        systemctl restart containerd
        systemctl enable containerd

    - name: Ensure Kubernetes apt key is installed
      ansible.builtin.apt_key:
        url: https://pkgs.k8s.io/core:/stable:/v1.30/deb/Release.key
        state: present

    - name: Ensure Kubernetes apt repository is present
      ansible.builtin.apt_repository:
        repo: "deb https://pkgs.k8s.io/core:/stable:/v1.30/deb/ /"
        state: present
        filename: kubernetes

    - name: Install kubelet, kubeadm, kubectl
      ansible.builtin.apt:
        name:
          - kubelet
          - kubeadm
          - kubectl # (omit this line on workers if you want)
          - python3-pip
        state: present
        update_cache: yes

    - name: Hold kubelet, kubeadm, kubectl at current version
      ansible.builtin.dpkg_selections:
        name: "{{ item }}"
        selection: hold
      loop:
        - kubelet
        - kubeadm
        - kubectl

    - name: Initialize control plane
      command: kubeadm init --pod-network-cidr=10.244.0.0/16
      args:
        creates: /etc/kubernetes/admin.conf

    - name: Create kube config directory
      file:
        path: /root/.kube
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Copy admin.conf to kube config
      copy:
        src: /etc/kubernetes/admin.conf
        dest: /root/.kube/config
        remote_src: true
        owner: root
        group: root
        mode: "0644"

    - name: Install calico CNI
      command: kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.30.2/manifests/calico.yaml
      environment:
        KUBECONFIG: /etc/kubernetes/admin.conf

    - name: Install local path provisioner
      command: kubectl apply -f https://raw.githubusercontent.com/rancher/local-path-provisioner/master/deploy/local-path-storage.yaml
      environment:
        KUBECONFIG: /etc/kubernetes/admin.conf

    - name: Get join command
      shell: kubeadm token create --print-join-command
      register: join_cmd

    - name: Save join command for workers
      copy:
        dest: /tmp/kube_join.sh
        content: |
          #!/bin/bash
          {{ join_cmd.stdout }}
        mode: "0755"

    - name: Install Docker
      ansible.builtin.import_tasks: docker.yml

    - name: Install Helm (used by the optional Olympus dashboard deploy step below)
      ansible.builtin.shell: |
        set -e
        if command -v helm >/dev/null 2>&1; then exit 0; fi
        curl -fsSL https://baltocdn.com/helm/signing.asc | gpg --dearmor -o /usr/share/keyrings/helm.gpg
        echo "deb [arch=amd64 signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" \
          > /etc/apt/sources.list.d/helm-stable-debian.list
        apt-get update
        apt-get install -y helm
      changed_when: false

- name: Configure Docker insecure registries
  hosts: master
  become: true
  vars:
    insecure_registries:
      - "{{ hostvars['master']['ansible_host'] }}:30500" # dynamically use your master node’s IP
  tasks:
    - name: Ensure /etc/docker directory exists
      file:
        path: /etc/docker
        state: directory
        mode: "0755"

    - name: Configure daemon.json with insecure registries
      copy:
        dest: /etc/docker/daemon.json
        content: |
          {
            "insecure-registries": [
            {% for reg in insecure_registries %}
              "{{ reg }}"{{ "," if not loop.last else "" }}
            {% endfor %}
            ]
          }
        mode: "0644"

    - name: Restart Docker
      systemd:
        name: docker
        state: restarted
        enabled: true

# Olympus deploy step — Helm-installs the dashboard chart from
# infra/k8s/charts/olympus once the control plane is up. Disabled by
# default so the user can review the cluster before workloads land.
#
# Required group_vars for this play (set in inventory or -e):
#   olympus_chart_path           # absolute path to infra/k8s/charts/olympus on the master
#   deployment_registry_host     # registry the dashboard image was pushed to
#   olympus_image_tag            # default: "dev"
#   anthropic_api_key            # optional; required if router=llm
#   openai_api_key               # optional
#
# - name: Deploy Olympus dashboard via Helm
#   hosts: master
#   become: true
#   tasks:
#     - name: Create provider-key Secret (idempotent)
#       command: >-
#         kubectl create secret generic olympus-secrets
#         --from-literal=anthropic_api_key={{ anthropic_api_key | default('') }}
#         --from-literal=openai_api_key={{ openai_api_key | default('') }}
#         --dry-run=client -o yaml
#       register: olympus_secret_yaml
#       changed_when: false
#     - name: Apply provider-key Secret
#       command: kubectl apply -f -
#       args:
#         stdin: "{{ olympus_secret_yaml.stdout }}"
#       environment:
#         KUBECONFIG: /etc/kubernetes/admin.conf
#     - name: helm install olympus
#       command: >-
#         helm upgrade --install olympus
#         {{ olympus_chart_path }}
#         --set image.repository={{ deployment_registry_host }}/olympus/dashboard
#         --set image.tag={{ olympus_image_tag | default('dev') }}
#       environment:
#         KUBECONFIG: /etc/kubernetes/admin.conf
