__pycache__/
*.pyc
*.pyo
.pytest_cache/
.olympus-data/
.venv/
*.egg-info/

# Local infra credentials — never commit
infra/env.sh
infra/deployment/
infra/terraform/deployment/
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.pem

# Frontend
agents/dashboard/frontend/node_modules
agents/dashboard/static/dist
agents/dashboard/frontend/tsconfig.tsbuildinfo
agents/dashboard/frontend/coverage

# pytest-cov runtime output
.coverage
.coverage.*
htmlcov/

# Long-form docs now live in the docs repo → docs.01p5.com (single source of truth)
/docs/

# Local Claude Code harness state (not shared)
.claude/

# all node_modules (root + nested)
node_modules/
